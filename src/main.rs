extern crate gmagick;

fn main() {

    // let src = std::env::args().nth(1).expect("Bad args");
    // let out = std::env::args().nth(2).expect("Bad args");

    // let dim = std::env::args().nth(3).iter()
    //     .flat_map(|x| x.split("x") )
    //     .map(|x| x.parse::<u64>().ok() )
    //     .collect::<Vec<_>>();

    // let mut img = gmagick::Image::from_path(&src)
    //     .and_then(|v| v.smart_scale(dim[0], dim[1]) )
    //     .expect("Could not read image");

    // println!("{:?}", "Done reading");

    // img.write(&out).expect("Could not write image");

}